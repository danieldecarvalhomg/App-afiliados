import { describe,expect,it } from 'vitest';import { validateImage } from './ImageFileValidator';
const png=()=>{const bytes=new Uint8Array(24);bytes.set([0x89,0x50,0x4e,0x47],0);bytes[19]=2;bytes[23]=2;return bytes;};
describe('validateImage',()=>{it('valida conteúdo PNG e calcula SHA-256',()=>{const value=validateImage(png(),'image/png');expect(value).toMatchObject({mimeType:'image/png',width:2,height:2});expect(value.contentHash).toMatch(/^[a-f0-9]{64}$/);});it('rejeita arquivo arbitrário',()=>expect(()=>validateImage(new Uint8Array([1,2,3]),'image/png')).toThrow('INVALID_IMAGE_MIME'));it('rejeita MIME falso',()=>expect(()=>validateImage(png(),'image/jpeg')).toThrow('MIME_MISMATCH'));it('rejeita arquivo grande',()=>expect(()=>validateImage(new Uint8Array(8*1024*1024+1),'image/png')).toThrow('IMAGE_TOO_LARGE'));});
