import{describe,expect,it}from'vitest';import{ConditionEngine}from'./ConditionEngine';import type{AutomationCondition,AutomationEventContext}from'./types';
const context:AutomationEventContext={sourceType:'whatsapp',sourceReferenceId:'x',marketplace:'shopee',price:249,originalPrice:399,discountPercent:38,couponExists:true,freeShipping:false,productTitle:'Echo Dot novo',category:'Eletrônicos',imageAvailable:true,affiliateConversionStatus:'converted'};
const c=(field:AutomationCondition['field'],operator:AutomationCondition['operator'],value?:AutomationCondition['value']):AutomationCondition=>({id:`${field}-${operator}`,field,operator,value});
describe('ConditionEngine determinístico',()=>{const engine=new ConditionEngine();it.each([[c('marketplace','equals','Shopee'),true],[c('discount_percent','greater_or_equal',30),true],[c('price','less_or_equal',500),true],[c('product_title','contains','echo'),true],[c('product_title','not_contains','usado'),true],[c('coupon_exists','is_true'),true],[c('free_shipping','is_false'),true],[c('original_price','exists'),true],[c('commission','not_exists'),true]] as const)('%o => %s',(condition,expected)=>expect(engine.evaluate(condition,context).passed).toBe(expected));it('aplica ALL e ANY',()=>{const rules=[c('marketplace','equals','shopee'),c('discount_percent','greater_or_equal',50)];expect(engine.evaluateAll(rules,'all',context).passed).toBe(false);expect(engine.evaluateAll(rules,'any',context).passed).toBe(true);});it('rejeita operador incompatível',()=>expect(()=>engine.evaluate(c('price','contains','2'),context)).toThrow('AUTOMATION_CONDITION_OPERATOR_INVALID'));});
