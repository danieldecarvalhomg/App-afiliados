import{describe,expect,it}from'vitest';import{CtaConditionEngine}from'./CtaConditionEngine';import type{CtaFacts}from'./types';const facts:CtaFacts={productId:'p',title:'Echo Dot',category:'Eletrônicos',price:249,originalPrice:399,discountPercent:37.59,couponCode:'ECHO20',couponDescription:null,couponLink:'https://cupom.test/echo',freeShipping:true,marketplace:'shopee',affiliateUrl:'https://s.shopee.com.br/affiliate',sourceUrl:'https://shopee.com.br/original'};const engine=new CtaConditionEngine();describe('CtaConditionEngine determinístico',()=>{it.each([[{field:'coupon',operator:'exists'},true],[{field:'coupon_link',operator:'exists'},true],[{field:'discount',operator:'gte',value:30},true],[{field:'price',operator:'lt',value:100},false],[{field:'original_price',operator:'exists'},true],[{field:'free_shipping',operator:'eq',value:true},true],[{field:'marketplace',operator:'eq',value:'amazon'},false]]as const)('avalia %o',(condition,expected)=>expect(engine.evaluate(condition,facts)).toBe(expected));it('link de cupom não depende de código de cupom',()=>expect(engine.evaluate({field:'coupon_link',operator:'exists'},{...facts,couponCode:null})).toBe(true));it('considera a descrição literal como cupom quando não há código',()=>expect(engine.evaluate({field:'coupon',operator:'exists'},{...facts,couponCode:null,couponDescription:'R$30 OFF'})).toBe(true));it('aplica AND e oculta bloco não aplicável',()=>expect(engine.isVisible({id:'x',key:'x',label:'x',kind:'factual',enabled:true,positionMode:'flexible',frequency:'always',conditions:[{field:'coupon',operator:'exists'},{field:'discount',operator:'gte',value:40}],format:'separate_line',groupId:null,generationInstruction:null,fixedText:null},facts)).toBe(false));it('não delega frequência never à IA',()=>expect(engine.isVisible({id:'x',key:'x',label:'x',kind:'creative',enabled:true,positionMode:'flexible',frequency:'never',conditions:[],format:'inline',groupId:null,generationInstruction:null,fixedText:null},facts)).toBe(false));});
